This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.docker/docker-compose.yml
.docker/grafana/datasources.yaml
.docker/loki.yaml
.docker/mimir.yaml
.docker/otel-collector.yaml
.docker/postgres/init/01_schema.sql
.docker/tempo.yaml
.github/workflows/gb-pages.yml
.github/workflows/publish-develop.yml
.github/workflows/release.yml
.gitignore
pom.xml
README.md
src/main/java/software/spool/crawler/api/builder/CrawlerBuilderFactory.java
src/main/java/software/spool/crawler/api/builder/CrawlerFacet.java
src/main/java/software/spool/crawler/api/builder/EventMappingSpecification.java
src/main/java/software/spool/crawler/api/builder/MappingFacet.java
src/main/java/software/spool/crawler/api/builder/ObservabilityFacet.java
src/main/java/software/spool/crawler/api/builder/PollingCrawlerAssembler.java
src/main/java/software/spool/crawler/api/builder/PollingCrawlerBuilder.java
src/main/java/software/spool/crawler/api/builder/SourceFacet.java
src/main/java/software/spool/crawler/api/Crawler.java
src/main/java/software/spool/crawler/api/port/InboxWriter.java
src/main/java/software/spool/crawler/api/port/PayloadSplitter.java
src/main/java/software/spool/crawler/api/port/source/PollSource.java
src/main/java/software/spool/crawler/api/port/source/Source.java
src/main/java/software/spool/crawler/api/port/source/StreamSource.java
src/main/java/software/spool/crawler/api/port/source/WebhookRoute.java
src/main/java/software/spool/crawler/api/port/source/WebhookSource.java
src/main/java/software/spool/crawler/api/strategy/CrawlerStrategy.java
src/main/java/software/spool/crawler/api/utils/CrawlerErrorRouter.java
src/main/java/software/spool/crawler/api/utils/CrawlerPorts.java
src/main/java/software/spool/crawler/api/utils/NormalizerFormat.java
src/main/java/software/spool/crawler/api/utils/StandardNormalizer.java
src/main/java/software/spool/crawler/internal/adapter/http/HTTPPollSource.java
src/main/java/software/spool/crawler/internal/adapter/sql/SQLPollSource.java
src/main/java/software/spool/crawler/internal/control/PayloadCapturedHandler.java
src/main/java/software/spool/crawler/internal/control/steps/BuildAndStoreEnvelopeStep.java
src/main/java/software/spool/crawler/internal/control/steps/BuildCapturedEventStep.java
src/main/java/software/spool/crawler/internal/control/steps/CapturedPayloadKeys.java
src/main/java/software/spool/crawler/internal/control/steps/PayloadSizeMetricStep.java
src/main/java/software/spool/crawler/internal/control/steps/PublishCapturedEvent.java
src/main/java/software/spool/crawler/internal/control/steps/PublishDomainEventStep.java
src/main/java/software/spool/crawler/internal/control/steps/PublishEnvelopeStoredStep.java
src/main/java/software/spool/crawler/internal/port/decorator/SafeInboxWriter.java
src/main/java/software/spool/crawler/internal/port/decorator/SafePayloadDeserializer.java
src/main/java/software/spool/crawler/internal/port/decorator/SafePayloadSplitter.java
src/main/java/software/spool/crawler/internal/port/decorator/SafePollSource.java
src/main/java/software/spool/crawler/internal/port/decorator/SafeRecordSerializer.java
src/main/java/software/spool/crawler/internal/strategy/PollingCrawlerStrategy.java
src/main/java/software/spool/crawler/internal/strategy/StreamCrawlerStrategy.java
src/main/java/software/spool/crawler/internal/utils/DomainEventEmitter.java
src/main/java/software/spool/crawler/internal/utils/factory/Normalizer.java
src/main/java/software/spool/crawler/internal/utils/factory/NormalizerFactory.java
src/main/java/software/spool/crawler/internal/utils/factory/PayloadSplitterFactory.java
src/main/java/software/spool/crawler/internal/utils/TypedDomainMapping.java
src/main/java/software/spool/crawler/Main.java
src/main/resources/Crawler.yaml
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".docker/postgres/init/01_schema.sql">
-- 01_schema.sql
-- Inbox ultra-minimal

CREATE SCHEMA IF NOT EXISTS spool_inbox;

CREATE TABLE spool_inbox.events (
                                    id BIGSERIAL PRIMARY KEY,
                                    source VARCHAR(255),
                                    status VARCHAR(20) NOT NULL DEFAULT 'PENDING',
                                    payload JSONB,
                                    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- Índices mínimos
CREATE INDEX idx_inbox_status ON spool_inbox.events(status);
CREATE INDEX idx_inbox_created ON spool_inbox.events(created_at);

-- Vista para eventos pendientes
CREATE VIEW spool_inbox.pending_events AS
SELECT * FROM spool_inbox.events
WHERE status = 'PENDING'
ORDER BY created_at ASC;

-- Función simple para marcar como DONE
CREATE OR REPLACE FUNCTION spool_inbox.mark_done(event_id BIGINT)
RETURNS VOID AS $$
BEGIN
UPDATE spool_inbox.events
SET status = 'DONE'
WHERE id = mark_done.event_id;
END;
$$ LANGUAGE plpgsql;

-- Función simple para marcar como FAILED
CREATE OR REPLACE FUNCTION spool_inbox.mark_failed(event_id BIGINT, new_status VARCHAR)
RETURNS VOID AS $$
BEGIN
UPDATE spool_inbox.events
SET status = new_status
WHERE id = mark_done.event_id;
END;
$$ LANGUAGE plpgsql;
</file>

<file path=".github/workflows/release.yml">
name: Release & Publish

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  release:
    uses: Spool-FRAMEWORK/.github/.github/workflows/reusable-release.yml@main
    secrets:
      GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
      GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
      MAVEN_CENTRAL_TOKEN_USERNAME: ${{ secrets.MAVEN_CENTRAL_TOKEN_USERNAME }}
      MAVEN_CENTRAL_TOKEN_PASSWORD: ${{ secrets.MAVEN_CENTRAL_TOKEN_PASSWORD }}
</file>

<file path="src/main/java/software/spool/crawler/api/builder/CrawlerFacet.java">
package software.spool.crawler.api.builder;

abstract class CrawlerFacet<I> {
    protected final PollingCrawlerBuilder<I> parent;

    protected CrawlerFacet(PollingCrawlerBuilder<I> parent) {
        this.parent = parent;
    }

    public PollingCrawlerBuilder<I> and() {
        return parent;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/builder/MappingFacet.java">
package software.spool.crawler.api.builder;

import software.spool.core.model.Event;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.port.serde.NamingConvention;

import java.util.function.BiFunction;

public class MappingFacet<I> extends CrawlerFacet<I> {

    private EventMappingSpecification spec;

    MappingFacet(PollingCrawlerBuilder<I> parent, EventMappingSpecification spec) {
        super(parent);
        this.spec = spec;
    }

    public MappingFacet<I> convention(NamingConvention convention) {
        this.spec = new EventMappingSpecification(convention);
        return this;
    }

    public MappingFacet<I> addDomainEvent(Class<? extends Event> eventType, String... partitionAttributes) {
        spec.addDomainEvent(eventType, partitionAttributes);
        return this;
    }

    public <D> MappingFacet<I> addDomainEvent(Class<D> dtoType, BiFunction<D, IdempotencyKey, Event> toEvent, String... partitionAttributes) {
        spec.addDomainEvent(dtoType, toEvent, partitionAttributes);
        return this;
    }

    public MappingFacet<I> addPartitionAttributes(String... attributes) {
        spec.addPartitionAttributes(attributes);
        return this;
    }

    EventMappingSpecification spec() {
        return spec;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/builder/ObservabilityFacet.java">
package software.spool.crawler.api.builder;

import software.spool.core.utils.routing.ErrorRouter;

public class ObservabilityFacet<I> extends CrawlerFacet<I> {

    ErrorRouter errorRouter;

    ObservabilityFacet(PollingCrawlerBuilder<I> parent) {
        super(parent);
    }

    public ObservabilityFacet<I> withErrorRouter(ErrorRouter errorRouter) {
        this.errorRouter = errorRouter;
        return this;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/builder/PollingCrawlerAssembler.java">
package software.spool.crawler.api.builder;

import software.spool.core.adapter.jackson.RecordSerializerFactory;
import software.spool.core.adapter.otel.OpenTelemetryMetricsRegistry;
import software.spool.core.pipeline.ObservedStep;
import software.spool.core.pipeline.Pipeline;
import software.spool.core.pipeline.PipelineContext;
import software.spool.core.port.bus.Handler;
import software.spool.core.port.metrics.MetricsRegistry;
import software.spool.core.utils.routing.ErrorRouter;
import software.spool.crawler.api.Crawler;
import software.spool.crawler.api.utils.CrawlerErrorRouter;
import software.spool.crawler.internal.control.PayloadCapturedHandler;
import software.spool.crawler.internal.control.steps.*;
import software.spool.crawler.internal.strategy.PollingCrawlerStrategy;
import software.spool.crawler.internal.utils.factory.Normalizer;

import java.util.Objects;

class PollingCrawlerAssembler<I> {

    private final PollingCrawlerBuilder<I> config;

    PollingCrawlerAssembler(PollingCrawlerBuilder<I> config) {
        this.config = config;
    }

    <P, E, R> Crawler assemble(Normalizer<P, E, R> normalizer) {
        config.sourceFacet.validate();
        return new Crawler(initializeStrategy(normalizer, initHandler()), getErrorRouter(), config.heartBeat);
    }

    private <P, E, R> PollingCrawlerStrategy<I, P, E, R> initializeStrategy(Normalizer<P, E, R> normalizer, Handler<byte[]> handler) {
        return new PollingCrawlerStrategy<>(config.source, normalizer, handler, config.sourceFacet.schedule, getErrorRouter());
    }

    private ErrorRouter getErrorRouter() {
        return Objects.requireNonNullElse(config.observabilityFacet.errorRouter, CrawlerErrorRouter.defaults(config.sourceFacet.ports.bus()));
    }

    private PayloadCapturedHandler initHandler() {
        return new PayloadCapturedHandler(initializePipeline(), config.source.sourceId(), config.sourceFacet.mediaType, getErrorRouter());
    }

    private Pipeline<PipelineContext, PipelineContext> initializePipeline() {
        EventMappingSpecification spec = config.mappingFacet.spec();
        return Pipeline.<PipelineContext>start()
                .add(new ObservedStep<>("measure-size", new PayloadSizeMetricStep(buildHistogram())))
                .add(new ObservedStep<>("build-captured", new BuildCapturedEventStep()))
                .add(new ObservedStep<>("emit-domain-event",
                        new PublishDomainEventStep(spec.buildEmitter(config.sourceFacet.ports.bus()))))
                .add(new ObservedStep<>("publish-captured", new PublishCapturedEvent(config.sourceFacet.ports.bus())))
                .add(new ObservedStep<>("store-envelope",
                        new BuildAndStoreEnvelopeStep(config.sourceFacet.ports.inboxWriter(),
                                RecordSerializerFactory.record(),
                                spec.partitionAttributes())))
                .add(new ObservedStep<>("publish-stored", new PublishEnvelopeStoredStep(config.sourceFacet.ports.bus())));
    }

    private MetricsRegistry.LongHistogramMetric buildHistogram() {
        return new OpenTelemetryMetricsRegistry()
                .histogram("spool.captured.payload.size", "", "By");
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/builder/SourceFacet.java">
package software.spool.crawler.api.builder;

import software.spool.core.model.vo.MediaType;
import software.spool.core.port.serde.EnrichmentRule;
import software.spool.core.utils.polling.PollingConfiguration;
import software.spool.crawler.api.utils.CrawlerPorts;

import java.time.Duration;
import java.util.List;
import java.util.Objects;

public class SourceFacet<I> extends CrawlerFacet<I> {
    CrawlerPorts ports;
    List<EnrichmentRule> enrichRules;
    String rootPath;
    PollingConfiguration schedule;
    MediaType mediaType;

    SourceFacet(PollingCrawlerBuilder<I> parent) {
        super(parent);
        this.schedule = PollingConfiguration.every(Duration.ofSeconds(30));
    }

    public SourceFacet<I> ports(CrawlerPorts ports) {
        this.ports = ports;
        return this;
    }

    public SourceFacet<I> schedule(PollingConfiguration config) {
        this.schedule = config;
        return this;
    }

    public SourceFacet<I> enrichRules(List<EnrichmentRule> enrichRules) {
        this.enrichRules = enrichRules;
        return this;
    }

    public SourceFacet<I> rootPath(String rootPath) {
        this.rootPath = rootPath;
        return this;
    }

    void validate() {
        Objects.requireNonNull(ports, "CrawlerPorts must be set before calling create()");
    }

    public SourceFacet<I> mediaType(MediaType mediaType) {
        this.mediaType = mediaType;
        return this;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/port/source/WebhookSource.java">
package software.spool.crawler.api.port.source;

/**
 * A data source that receives data by exposing an HTTP webhook endpoint.
 *
 * <p>
 * Implementations must provide a {@link WebhookRoute} through
 * {@link #bindRoute()} so that the crawler runtime can register the appropriate
 * HTTP handler and forward incoming requests to the processing pipeline.
 * </p>
 */
public interface WebhookSource extends Source {
    /**
     * Returns the route configuration that defines how the webhook endpoint is
     * registered in the HTTP framework.
     *
     * @return the {@link WebhookRoute} for this source; must not be {@code null}
     */
    WebhookRoute bindRoute();
}
</file>

<file path="src/main/java/software/spool/crawler/api/utils/StandardNormalizer.java">
package software.spool.crawler.api.utils;

import com.fasterxml.jackson.databind.JsonNode;
import software.spool.core.port.serde.EnrichmentRule;
import software.spool.crawler.api.port.source.PollSource;
import software.spool.crawler.internal.utils.factory.Normalizer;
import software.spool.crawler.internal.utils.factory.NormalizerFactory;

import java.sql.ResultSet;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.Objects;

/**
 * Predefined {@link NormalizerFormat} constants ready to be used with the
 * crawler DSL.
 *
 * <p>
 * Each constant wires together a deserializer, a splitter and a serializer
 * via the corresponding
 * {@link NormalizerFactory}
 * factory method. Choose the constant that matches the raw data produced by
 * your {@link PollSource}.
 * </p>
 *
 * <p>
 * Example usage:
 * </p>
 *
 * <pre>{@code
 * Crawlers.poll(mySource)
 *                 .withFormat(Formats.JSON_ARRAY)
 *                 .create();
 * }</pre>
 */
public final class StandardNormalizer {

        private StandardNormalizer() {
        }

        public static final NormalizerFormat<JsonNode, JsonNode, JsonNode> JSON_OBJECT = NormalizerFactory::jsonObject;
        public static final NormalizerFormat<JsonNode, JsonNode, JsonNode> JSON_ARRAY = NormalizerFactory::jsonArray;
        public static final NormalizerFormat<JsonNode, JsonNode, JsonNode> YAML_ARRAY = NormalizerFactory::yamlArray;
        public static final NormalizerFormat<ResultSet, Map<String, Object>, Map<String, Object>> RESULT_SET = (r, p) -> NormalizerFactory.resultSet();

        public static class Builder {
                private List<EnrichmentRule> enrichRules;
                private String rootPath;

                public Builder() {
                        this.enrichRules = new ArrayList<>();
                        this.rootPath = "";
                }

                public Builder enrichRules(List<EnrichmentRule> enrichRules) {
                        this.enrichRules = Objects.requireNonNullElse(enrichRules, new ArrayList<>());
                        return this;
                }

                public Builder rootPath(String rootPath) {
                        this.rootPath = Objects.requireNonNullElse(rootPath, "");
                        return this;
                }

                public Normalizer<?, ?, ?> valueOf(Format format) {
                        return switch (format) {
                                case JSON_OBJECT -> JSON_OBJECT.pipelineWith(enrichRules, rootPath);
                                case JSON_ARRAY  -> JSON_ARRAY.pipelineWith(enrichRules, rootPath);
                                case YAML_ARRAY  -> YAML_ARRAY.pipelineWith(enrichRules, rootPath);
                                case RESULT_SET  -> RESULT_SET.pipelineWith(enrichRules, rootPath);
                                default -> throw new IllegalArgumentException("Unknown format: " + format);
                        };
                }
        }

        public static enum Format {
                JSON_ARRAY, YAML_ARRAY, RESULT_SET, JSON_OBJECT
        }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/steps/BuildCapturedEventStep.java">
package software.spool.crawler.internal.control.steps;

import software.spool.core.model.event.SourcePayloadCaptured;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.pipeline.PipelineContext;
import software.spool.core.pipeline.Step;

import javax.management.AttributeNotFoundException;
import java.util.UUID;

public class BuildCapturedEventStep implements Step<PipelineContext, PipelineContext> {
    @Override
    public PipelineContext apply(PipelineContext ctx) throws AttributeNotFoundException {
        SourcePayloadCaptured captured = SourcePayloadCaptured.builder()
                .idempotencyKey(IdempotencyKey.of(ctx.require(CapturedPayloadKeys.SOURCE_ID),
                        ctx.require(CapturedPayloadKeys.PAYLOAD)))
                .correlationId(UUID.randomUUID().toString())
                .build();
        return ctx.with(CapturedPayloadKeys.CAPTURED_EVENT, captured);
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/steps/PublishDomainEventStep.java">
package software.spool.crawler.internal.control.steps;

import software.spool.core.pipeline.PipelineContext;
import software.spool.core.pipeline.Step;
import software.spool.crawler.internal.utils.DomainEventEmitter;
import software.spool.crawler.internal.utils.TypedDomainMapping;

import javax.management.AttributeNotFoundException;
import java.util.Optional;

public class PublishDomainEventStep implements Step<PipelineContext, PipelineContext> {
    private final DomainEventEmitter domainEventEmitter;

    public PublishDomainEventStep(DomainEventEmitter domainEventEmitter) {
        this.domainEventEmitter = domainEventEmitter;
    }

    @Override
    public PipelineContext apply(PipelineContext ctx) throws AttributeNotFoundException {
        Optional<TypedDomainMapping> matched = domainEventEmitter.emit(
                ctx.require(CapturedPayloadKeys.PAYLOAD),
                ctx.require(CapturedPayloadKeys.CAPTURED_EVENT).idempotencyKey());
        return ctx.with(CapturedPayloadKeys.DOMAIN_MAPPING, matched);
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/port/decorator/SafePayloadSplitter.java">
package software.spool.crawler.internal.port.decorator;

import software.spool.core.exception.SplitException;
import software.spool.core.exception.SpoolException;
import software.spool.crawler.api.port.PayloadSplitter;

import java.util.stream.Stream;

/**
 * Decorator for {@link PayloadSplitter} that normalises unchecked exceptions
 * into typed {@link SplitException} instances.
 *
 * <p>
 * If the delegate's {@link #split(Object)} method throws a
 * {@link SpoolException} subclass, it is re-thrown as-is. Any other
 * {@link Exception} is wrapped in a new {@link SplitException}.
 * </p>
 *
 * @param <I> the intermediate input type
 * @param <O> the individual record output type
 */
public class SafePayloadSplitter<I, O> implements PayloadSplitter<I, O> {
    private final PayloadSplitter<I, O> splitter;

    private SafePayloadSplitter(PayloadSplitter<I, O> splitter) {
        this.splitter = splitter;
    }

    /**
     * Creates a new {@code SafeSourceSplitter} wrapping the given delegate.
     *
     * @param <I>      the input type
     * @param <O>      the output type
     * @param splitter the splitter to wrap; must not be {@code null}
     * @return a new {@code SafeSourceSplitter} instance
     */
    public static <I, O> SafePayloadSplitter<I, O> of(PayloadSplitter<I, O> splitter) {
        return new SafePayloadSplitter<>(splitter);
    }

    @Override
    public Stream<O> split(I payload) throws SpoolException {
        try {
            return splitter.split(payload);
        } catch (SpoolException e) {
            throw e;
        } catch (Exception e) {
            throw new SplitException(payload.toString(), e.getMessage());
        }
    }
}
</file>

<file path=".docker/grafana/datasources.yaml">
apiVersion: 1
datasources:
  - name: Tempo
    type: tempo
    url: http://spool-tempo:3200
    isDefault: true
  - name: Loki
    type: loki
    url: http://spool-loki:3100
  - name: Mimir
    type: prometheus
    access: proxy
    url: http://mimir:9009/prometheus
    jsonData:
      prometheusType: Mimir
      httpMethod: POST
</file>

<file path=".docker/loki.yaml">
auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096

common:
  instance_addr: 127.0.0.1
  path_prefix: /tmp/loki
  storage:
    filesystem:
      chunks_directory: /tmp/loki/chunks
      rules_directory: /tmp/loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

limits_config:
  allow_structured_metadata: true
  volume_enabled: true
  otlp_config:
    resource_attributes:
      attributes_config:
        - action: index_label
          attributes:
            - service.name

schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h
</file>

<file path=".docker/mimir.yaml">
multitenancy_enabled: false

server:
  http_listen_port: 9009
  log_level: info

common:
  storage:
    backend: filesystem

blocks_storage:
  backend: filesystem
  filesystem:
    dir: /tmp/mimir/blocks-storage

compactor:
  data_dir: /tmp/mimir/compactor

ingester:
  ring:
    replication_factor: 1

ruler_storage:
  backend: filesystem
  filesystem:
    dir: /tmp/mimir/rules

store_gateway:
  sharding_ring:
    replication_factor: 1
</file>

<file path=".docker/otel-collector.yaml">
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:

exporters:
  debug:
    verbosity: detailed

  prometheusremotewrite:
    endpoint: http://mimir:9009/api/v1/push
    resource_to_telemetry_conversion:
      enabled: true

service:
  pipelines:
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug, prometheusremotewrite]
</file>

<file path=".docker/tempo.yaml">
server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: 0.0.0.0:4317
        http:
          endpoint: 0.0.0.0:4318

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/blocks
    wal:
      path: /tmp/tempo/wal
</file>

<file path=".gitignore">
### Maven ###
target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### IntelliJ IDEA ###
.idea/
*.iws
*.iml
*.ipr

### Eclipse ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

### OS ###
.DS_Store
Thumbs.db
Desktop.ini

### Logs ###
*.log
logs/

### Secretos y configuración local ###
.env
.env.*
*.local.properties
application-local.yml
application-local.properties
secrets/

crawler.xml
</file>

<file path="src/main/java/software/spool/crawler/api/port/source/Source.java">
package software.spool.crawler.api.port.source;

import software.spool.core.exception.SpoolException;

/**
 * Base internal interface for all types of data sources.
 *
 * <p>
 * {@code Source} extends {@link AutoCloseable} so that sources can be used
 * inside try-with-resources blocks. The {@link #close()} method is a no-op by
 * default; implementors should override it when resources need to be released
 * after a polling or streaming cycle completes.
 * </p>
 *
 * <p>
 * This is an SPI interface and is not intended for direct implementation
 * by end users. Use the higher-level
 * {@link PollSource},
 * {@link StreamSource}, or
 * {@link WebhookSource} instead.
 * </p>
 */
public interface Source<R> extends AutoCloseable {
    /**
     * Returns the unique identifier of this source.
     *
     * @return a non-null string that uniquely identifies this source instance
     */
    String sourceId();

    R fetch() throws SpoolException;

    /**
     * Releases any resources held by this source.
     *
     * <p>
     * The default implementation is a no-op. Override to close connections,
     * streams, or other resources.
     * </p>
     */
    default void close() {
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/port/source/WebhookRoute.java">
package software.spool.crawler.api.port.source;

/**
 * Configuration descriptor for registering a webhook HTTP endpoint.
 *
 * <p>
 * A {@link WebhookSource} returns an instance of this class from
 * {@link WebhookSource#bindRoute()} to tell the crawler runtime which HTTP
 * method, path, and other routing options should be used when exposing the
 * webhook. Extend or populate this class with the fields required by your
 * HTTP framework integration.
 * </p>
 */
public class WebhookRoute {
    /** Creates a new {@code WebhookRoute}. */
    public WebhookRoute() {
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/utils/NormalizerFormat.java">
package software.spool.crawler.api.utils;

import software.spool.core.port.serde.EnrichmentRule;
import software.spool.crawler.internal.utils.factory.Normalizer;

import java.util.List;

@FunctionalInterface
public interface NormalizerFormat<P, E, R> {
    Normalizer<P, E, R> pipelineWith(List<EnrichmentRule> enrichmentRules, String rootPath);
}
</file>

<file path="src/main/java/software/spool/crawler/internal/adapter/http/HTTPPollSource.java">
package software.spool.crawler.internal.adapter.http;

import software.spool.core.exception.SpoolException;
import software.spool.crawler.api.port.source.PollSource;

import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;
import java.time.Duration;

public class HTTPPollSource implements PollSource<String> {
    private final HttpClient httpClient;
    private final String url;
    private final String sourceId;

    public HTTPPollSource(String url, String sourceId) {
        this.url = url;
        this.sourceId = sourceId;
        this.httpClient = HttpClient.newBuilder()
                .connectTimeout(Duration.ofSeconds(5))
                .build();
    }

    @Override
    public String fetch() throws SpoolException {
        try {
            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create(url))
                    .timeout(Duration.ofSeconds(10))
                    .header("Accept", "application/json")
                    .GET()
                    .build();

            HttpResponse<String> response = httpClient.send(
                    request, HttpResponse.BodyHandlers.ofString()
            );

            if (response.statusCode() != 200) {
                throw new RuntimeException(
                        "Gemini API returned HTTP " + response.statusCode()
                );
            }
            return response.body();
        } catch (SpoolException e) {
            throw e;
        } catch (Exception e) {
            throw new RuntimeException("Error polling: " + e.getMessage(), e);
        }
    }

    @Override
    public String sourceId() {
        return sourceId;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/adapter/sql/SQLPollSource.java">
package software.spool.crawler.internal.adapter.sql;

import software.spool.core.exception.SourcePollException;
import software.spool.core.exception.SpoolException;
import software.spool.crawler.api.port.source.PollSource;

import java.sql.*;
import java.util.List;
import java.util.Map;

public class SQLPollSource implements PollSource<ResultSet>, AutoCloseable {

    public enum DatabaseType {
        POSTGRESQL,
        MYSQL,
        MARIADB,
        SQLSERVER
    }

    private final String sourceId;
    private final DatabaseType databaseType;
    private final String host;
    private final int port;
    private final String databaseName;
    private final String username;
    private final String password;
    private final String query;
    private final List<Object> params;
    private final Map<String, String> properties;

    private Connection connection;
    private PreparedStatement statement;
    private ResultSet resultSet;

    public SQLPollSource(
            String sourceId,
            DatabaseType databaseType,
            String host,
            int port,
            String databaseName,
            String username,
            String password,
            String query,
            List<Object> params,
            Map<String, String> properties
    ) {
        this.sourceId = sourceId;
        this.databaseType = databaseType;
        this.host = host;
        this.port = port;
        this.databaseName = databaseName;
        this.username = username;
        this.password = password;
        this.query = query;
        this.params = params;
        this.properties = properties;
    }

    @Override
    public ResultSet fetch() throws SpoolException {
        try {
            closeCurrentResources();

            String jdbcUrl = buildJdbcUrl();
            connection = DriverManager.getConnection(jdbcUrl, username, password);
            statement = connection.prepareStatement(query);

            if (params != null) {
                for (int i = 0; i < params.size(); i++) {
                    statement.setObject(i + 1, params.get(i));
                }
            }

            resultSet = statement.executeQuery();
            return resultSet;

        } catch (SQLException e) {
            closeSilently();
            throw new SourcePollException(sourceId, e.getMessage());
        }
    }

    @Override
    public String sourceId() {
        return sourceId;
    }

    private String buildJdbcUrl() {
        String baseUrl = switch (databaseType) {
            case POSTGRESQL -> "jdbc:postgresql://%s:%d/%s".formatted(host, port, databaseName);
            case MYSQL -> "jdbc:mysql://%s:%d/%s".formatted(host, port, databaseName);
            case MARIADB -> "jdbc:mariadb://%s:%d/%s".formatted(host, port, databaseName);
            case SQLSERVER -> "jdbc:sqlserver://%s:%d;databaseName=%s"
                    .formatted(host, port, databaseName);
        };

        if (properties == null || properties.isEmpty()) {
            return baseUrl;
        }

        String separator = databaseType == DatabaseType.SQLSERVER ? ";" : "?";
        String joiner = databaseType == DatabaseType.SQLSERVER ? ";" : "&";

        String props = properties.entrySet().stream()
                .map(e -> e.getKey() + "=" + e.getValue())
                .reduce((a, b) -> a + joiner + b)
                .orElse("");

        return baseUrl + separator + props;
    }

    @Override
    public void close() throws SpoolException {
        try {
            closeCurrentResources();
        } catch (SQLException e) {
            throw new SourcePollException(sourceId, "Error closing SQL resources for source '%s'"
                    .formatted(sourceId));
        }
    }

    private void closeCurrentResources() throws SQLException {
        if (resultSet != null && !resultSet.isClosed()) {
            resultSet.close();
        }
        if (statement != null && !statement.isClosed()) {
            statement.close();
        }
        if (connection != null && !connection.isClosed()) {
            connection.close();
        }

        resultSet = null;
        statement = null;
        connection = null;
    }

    private void closeSilently() {
        try {
            closeCurrentResources();
        } catch (SQLException ignored) {
        }
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/steps/CapturedPayloadKeys.java">
package software.spool.crawler.internal.control.steps;

import software.spool.core.model.event.SourcePayloadCaptured;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.model.vo.MediaType;
import software.spool.core.pipeline.ContextKey;
import software.spool.crawler.internal.utils.TypedDomainMapping;

import java.util.Optional;

public final class CapturedPayloadKeys {
    private CapturedPayloadKeys() {}

    public static final ContextKey<String> SOURCE_ID = ContextKey.of("sourceId");
    public static final ContextKey<byte[]> PAYLOAD = ContextKey.of("payload");
    public static final ContextKey<MediaType> MEDIA_TYPE = ContextKey.of("mediaType");
    public static final ContextKey<SourcePayloadCaptured> CAPTURED_EVENT = ContextKey.of("capturedEvent");
    public static final ContextKey<Optional<TypedDomainMapping>> DOMAIN_MAPPING = ContextKey.of("domainMapping");
    public static final ContextKey<IdempotencyKey> RECEIVED_KEY = ContextKey.of("receivedKey");
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/steps/PayloadSizeMetricStep.java">
package software.spool.crawler.internal.control.steps;

import software.spool.core.pipeline.PipelineContext;
import software.spool.core.pipeline.Step;
import software.spool.core.port.metrics.MetricsRegistry;

import javax.management.AttributeNotFoundException;
import java.util.Map;

public class PayloadSizeMetricStep implements Step<PipelineContext, PipelineContext> {
    private final MetricsRegistry.LongHistogramMetric histogram;

    public PayloadSizeMetricStep(MetricsRegistry.LongHistogramMetric histogram) {
        this.histogram = histogram;
    }

    @Override
    public PipelineContext apply(PipelineContext ctx) throws AttributeNotFoundException {
        byte[] payload = ctx.require(CapturedPayloadKeys.PAYLOAD);
        histogram.record(payload.length, Map.of());
        return ctx;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/steps/PublishCapturedEvent.java">
package software.spool.crawler.internal.control.steps;

import software.spool.core.model.event.SourcePayloadCaptured;
import software.spool.core.pipeline.PipelineContext;
import software.spool.core.pipeline.Step;
import software.spool.core.port.bus.BrokerMessage;
import software.spool.core.port.bus.Destination;
import software.spool.core.port.bus.EventPublisher;

import javax.management.AttributeNotFoundException;
import java.util.Map;

public class PublishCapturedEvent implements Step<PipelineContext, PipelineContext> {
    private final EventPublisher publisher;

    public PublishCapturedEvent(EventPublisher publisher) {
        this.publisher = publisher;
    }

    @Override
    public PipelineContext apply(PipelineContext ctx) throws AttributeNotFoundException {
        SourcePayloadCaptured event = ctx.require(CapturedPayloadKeys.CAPTURED_EVENT);
        publisher.publish(event);
        return ctx;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/steps/PublishEnvelopeStoredStep.java">
package software.spool.crawler.internal.control.steps;

import software.spool.core.model.event.EnvelopeStored;
import software.spool.core.pipeline.PipelineContext;
import software.spool.core.pipeline.Step;
import software.spool.core.port.bus.EventPublisher;

import javax.management.AttributeNotFoundException;

public class PublishEnvelopeStoredStep implements Step<PipelineContext, PipelineContext> {
    private final EventPublisher publisher;

    public PublishEnvelopeStoredStep(EventPublisher publisher) {
        this.publisher = publisher;
    }

    @Override
    public PipelineContext apply(PipelineContext ctx) throws AttributeNotFoundException {
        EnvelopeStored storedEvent = EnvelopeStored.builder()
                .from(ctx.require(CapturedPayloadKeys.CAPTURED_EVENT))
                .build();
        publisher.publish(storedEvent);
        return ctx;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/utils/factory/NormalizerFactory.java">
package software.spool.crawler.internal.utils.factory;

import com.fasterxml.jackson.databind.JsonNode;
import software.spool.core.adapter.jackson.*;
import software.spool.core.port.serde.*;
import software.spool.crawler.api.port.PayloadSplitter;
import software.spool.crawler.api.utils.StandardNormalizer;

import java.sql.ResultSet;
import java.util.List;
import java.util.Map;

/**
 * Factory class that assembles pre-configured {@link Normalizer} instances for
 * common data formats.
 *
 * <p>
 * The factory methods are referenced directly from the constants in
 * {@link StandardNormalizer} as method references:
 * 
 * <pre>{@code
 * ProcessorFormat<String, JsonNode, JsonNode> JSON_ARRAY = NormalizerFactory::jsonArray;
 * }</pre>
 *
 * <p>
 * Custom transformers can be created with the generic variant.
 * </p>
 */
public class NormalizerFactory {
    private NormalizerFactory() {}

    public static Normalizer<JsonNode, JsonNode, JsonNode> jsonArray(List<EnrichmentRule> rules, String rootPath) {
        return new Normalizer<>(
                PayloadDeserializerFactory.json().asNode(),
                PayloadExtractorFactory.withRules(rules),
                PayloadLocatorFactory.fromRootPath(rootPath),
                PayloadSplitterFactory.jsonArray(),
                RecordEnricherFactory.json(),
                RecordSerializerFactory.jsonNode());
    }

    public static Normalizer<JsonNode, JsonNode, JsonNode> yamlArray(List<EnrichmentRule> rules, String rootPath) {
        return new Normalizer<>(
                PayloadDeserializerFactory.yaml().asNode(),
                PayloadExtractorFactory.withRules(rules),
                PayloadLocatorFactory.fromRootPath(rootPath),
                PayloadSplitterFactory.jsonArray(),
                RecordEnricherFactory.json(),
                RecordSerializerFactory.jsonNode());
    }

    public static Normalizer<JsonNode, JsonNode, JsonNode> jsonObject(List<EnrichmentRule> rules, String rootPath) {
        return new Normalizer<>(
                PayloadDeserializerFactory.json().asNode(),
                PayloadExtractorFactory.withRules(rules),
                PayloadLocatorFactory.fromRootPath(rootPath),
                PayloadSplitterFactory.single(),
                RecordEnricherFactory.json(),
                RecordSerializerFactory.jsonNode());
    }

    public static Normalizer<ResultSet, Map<String, Object>, Map<String, Object>> resultSet() {
        return new Normalizer<>(
                PayloadDeserializerFactory.<ResultSet>noOp(),
                PayloadExtractorFactory.<ResultSet, Map<String, Object>>noOp(),
                PayloadLocatorFactory.<ResultSet>noOp(),
                PayloadSplitterFactory.resultSet(),
                RecordEnricherFactory.<Map<String, Object>>noOp(),
                RecordSerializerFactory.map());
    }

    public static <P, E, R> Normalizer<P, E, R> of(
            PayloadDeserializer<P> deserializer,
            PayloadExtractor<P, E> extractor,
            PayloadLocator<P> locator,
            PayloadSplitter<P, R> splitter,
            RecordEnricher<R, E> enricher,
            RecordSerializer<R> serializer) {
        return new Normalizer<>(deserializer, extractor, locator, splitter, enricher, serializer);
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/port/PayloadSplitter.java">
package software.spool.crawler.api.port;

import software.spool.core.exception.SpoolException;
import software.spool.crawler.internal.utils.factory.PayloadSplitterFactory;

import java.util.stream.Stream;

/**
 * Internal SPI for splitting a parsed intermediate object into zero or more
 * individual records.
 *
 * <p>
 * This is the second stage of the three-stage processing pipeline:
 * deserialize → <b>split</b> → serialize.
 * </p>
 *
 * <p>
 * Pre-built implementations are available via
 * {@link PayloadSplitterFactory}.
 * </p>
 *
 * @param <P> the intermediate type produced by the deserializer
 * @param <R> the individual record type produced by splitting
 */
@FunctionalInterface
public interface PayloadSplitter<P, R> {
    /**
     * Splits the parsed payload into a stream of individual records.
     *
     * @param payload  the intermediate data to split; must not be {@code null}
     * @return a non-null (possibly empty) {@link Stream} of individual records
     * @throws SpoolException if the payload structure is incompatible with this
     *                        splitter
     */
    Stream<R> split(P payload) throws SpoolException;
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/PayloadCapturedHandler.java">
package software.spool.crawler.internal.control;

import software.spool.core.model.vo.MediaType;
import software.spool.core.pipeline.Pipeline;
import software.spool.core.pipeline.PipelineContext;
import software.spool.core.port.bus.Handler;
import software.spool.core.utils.routing.ErrorRouter;
import software.spool.crawler.internal.control.steps.CapturedPayloadKeys;

public class PayloadCapturedHandler implements Handler<byte[]> {
    private final Pipeline<PipelineContext, PipelineContext> pipeline;
    private final String sourceId;
    private final MediaType mediaType;
    private final ErrorRouter errorRouter;

    public PayloadCapturedHandler(Pipeline<PipelineContext, PipelineContext> pipeline, String sourceId, MediaType mediaType, ErrorRouter errorRouter) {
        this.pipeline = pipeline;
        this.sourceId = sourceId;
        this.mediaType = mediaType;
        this.errorRouter = errorRouter;
    }

    @Override
    public void handle(byte[] payload) {
        PipelineContext initial = PipelineContext.empty()
                .with(CapturedPayloadKeys.SOURCE_ID, sourceId)
                .with(CapturedPayloadKeys.MEDIA_TYPE, mediaType)
                .with(CapturedPayloadKeys.PAYLOAD, payload);
        pipeline.execute(initial)
                .peekError(errorRouter::dispatch);
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/port/decorator/SafeRecordSerializer.java">
package software.spool.crawler.internal.port.decorator;

import software.spool.core.exception.SerializationException;
import software.spool.core.exception.SpoolException;
import software.spool.core.port.serde.RecordSerializer;

/**
 * Decorator for {@link RecordSerializer} that normalises unchecked exceptions
 * into typed {@link SerializationException} instances.
 *
 * <p>
 * If the delegate's {@link #serialize(Object)} method throws a
 * {@link SpoolException} subclass, it is re-thrown as-is. Any other
 * {@link Exception} is wrapped in a new {@link SerializationException}.
 * </p>
 *
 * @param <T> the individual record type to serialize
 */
public class SafeRecordSerializer<T> implements RecordSerializer<T> {
    private final RecordSerializer<T> serializer;

    private SafeRecordSerializer(RecordSerializer<T> serializer) {
        this.serializer = serializer;
    }

    /**
     * Creates a new {@code SafeRecordSerializer} wrapping the given delegate.
     *
     * @param <T>        the record type
     * @param serializer the serializer to wrap; must not be {@code null}
     * @return a new {@code SafeRecordSerializer} instance
     */
    public static <T> SafeRecordSerializer<T> of(RecordSerializer<T> serializer) {
        return new SafeRecordSerializer<>(serializer);
    }

    @Override
    public byte[] serialize(T record) throws SpoolException {
        try {
            return serializer.serialize(record);
        } catch (SpoolException e) {
            throw e;
        } catch (Exception e) {
            throw new SerializationException(record.toString(), e.getMessage());
        }
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/strategy/StreamCrawlerStrategy.java">
package software.spool.crawler.internal.strategy;

import software.spool.core.exception.SpoolException;
import software.spool.core.port.bus.Handler;
import software.spool.core.utils.polling.CancellationToken;
import software.spool.core.utils.routing.ErrorRouter;
import software.spool.crawler.api.port.source.StreamSource;
import software.spool.crawler.api.strategy.CrawlerStrategy;
import software.spool.crawler.internal.utils.factory.Normalizer;

public class StreamCrawlerStrategy<I, P, E, R> implements CrawlerStrategy {
    private final StreamSource<I> source;
    private final Normalizer<P, E, R> normalizer;
    private final ErrorRouter errorRouter;
    private final Handler<byte[]> payloadCapturedHandler;

    public StreamCrawlerStrategy(StreamSource<I> source, Normalizer<P, E, R> normalizer, ErrorRouter errorRouter, Handler<byte[]> payloadCapturedHandler) {
        this.source = source;
        this.normalizer = normalizer;
        this.errorRouter = errorRouter;
        this.payloadCapturedHandler = payloadCapturedHandler;
    }

    @Override
    public void execute(CancellationToken token) throws SpoolException {
        source.start(
                m -> {
                    if (token.isCancelled()) return;
                    normalizer.normalize(m)
                            .takeWhile(p -> token.isActive())
                            .forEach(payloadCapturedHandler::handle);
                },
                e -> {
                    if (token.isCancelled()) return;
                    errorRouter.dispatch(e);
                }
        );
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/utils/factory/Normalizer.java">
package software.spool.crawler.internal.utils.factory;

import software.spool.core.port.serde.*;
import software.spool.crawler.api.port.PayloadSplitter;
import java.util.stream.Stream;

/**
 * Immutable record that composes the three processing stages of the crawler
 * pipeline into a single value object.
 *
 * <p>
 * The three stages are applied in order during each execution cycle:
 * <ol>
 * <li>{@link #deserializer()} – converts the raw source output into an
 * intermediate typed object.</li>
 * <li>{@link #splitter()} – expands the intermediate object into zero or more
 * individual records.</li>
 * <li>{@link #serializer()} – converts each record into a {@code String}
 * payload
 * ready for inbox storage.</li>
 * </ol>
 *
 * <p>
 * Pre-configured instances are available from {@link NormalizerFactory}.
 * For custom assembly use one of the static factory methods:
 * </p>
 * 
 * <pre>{@code
 * Normalizer<String, JsonNode, JsonNode> t = Normalizer.of(myDeserializer, mySplitter, mySerializer);
 * }</pre>
 *
 * @param deserializer the deserialization stage
 * @param splitter     the splitting stage
 * @param serializer   the serialization stage
 */
public record Normalizer<P, E, R>(
        PayloadDeserializer<P> deserializer,
        PayloadExtractor<P, E> extractor,
        PayloadLocator<P> locator,
        PayloadSplitter<P, R> splitter,
        RecordEnricher<R, E> enricher,
        RecordSerializer<R> serializer) {
    /**
     * Creates a new {@code Normalizer} from the three given pipeline components.
     *
     * @param <P>          the intermediate parsed type
     * @param <R>          the individual record type
     * @param deserializer the deserializer stage; must not be {@code null}
     * @param splitter     the splitter stage; must not be {@code null}
     * @param serializer   the serializer stage; must not be {@code null}
     * @return a new {@code Normalizer}
     */
    public static <P, E, R> Normalizer<P, E, R> of(PayloadDeserializer<P> deserializer, PayloadExtractor<P, E> extractor, PayloadLocator<P> locator,
                                             PayloadSplitter<P, R> splitter, RecordEnricher<R, E> enricher, RecordSerializer<R> serializer) {
        return new Normalizer<>(deserializer, extractor, locator, splitter, enricher, serializer);
    }

    @SuppressWarnings("unchecked")
    public <I> Stream<byte[]> normalize(I poll) {
        P parsed = poll instanceof byte[] raw ? deserializer.deserialize(raw) : (P) poll;
        return enricher.enrich(splitter.split(locator.locate(parsed)), extractor.extract(parsed))
                .map(serializer::serialize);
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/utils/TypedDomainMapping.java">
package software.spool.crawler.internal.utils;


import software.spool.core.utils.serialization.DomainEventMapping;

import java.util.List;

public record TypedDomainMapping(Class<?> targetType, DomainEventMapping<?> mapping, List<String> partitionAttributes) {
    public static TypedDomainMapping of(Class<?> type, DomainEventMapping<?> mapping, String... partitionAttributes) {
        return new TypedDomainMapping(type, mapping, List.of(partitionAttributes));
    }
}
</file>

<file path="src/main/resources/Crawler.yaml">
infrastructure:
  watchdog:
    url: "http://localhost:8090"
  
  eventBus:
    type: "IN_MEMORY"
  
  inbox:
    sql:
      type: "postgresql"
      host: "localhost"
      database: "spool"
      user: "spool"
      password: "spool"

  dataLake: {}

spoolModuleList:
  - crawler:
      id: "gemini-market"
      source:
        id: "gemini-market"
        poll:
          http:
            url: "https://api.gemini.com/v2/ticker/BTCUSD"
          schedule:
            every: 5
            unit: "SECONDS"
        format: "JSON_OBJECT"
      eventMapping:
        namingConvention: "SNAKE_CASE"
        attributeList:
          - value: "symbol"

  - crawler:
      id: "gemini-market-v2"
      source:
        id: "gemini-market"
        poll:
          http:
            url: "https://api.gemini.com/v1/trades/BTCUSD?limit_trades=100"
          schedule:
            every: 60
            unit: "SECONDS"
        format: "JSON_ARRAY"
      eventMapping:
        namingConvention: "SNAKE_CASE"
        attributeList:
          - value: "symbol"

  - feeder:
    id: "gemini-feeder"
    workers: 4
    poll:
      every: 5
      unit: "SECONDS"
</file>

<file path="README.md">
# Spool — Crawler

[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](https://www.apache.org/licenses/LICENSE-2.0)
[![Java](https://img.shields.io/badge/Java-17%2B-orange.svg)](https://openjdk.org/)
[![Maven](https://img.shields.io/badge/build-Maven-C71A36.svg)](https://maven.apache.org/)

**Crawler** is the **Spool** framework module responsible for **fetching raw data from external sources and delivering it to the inbox**. It handles the full polling pipeline — fetch → deserialize → split → serialize → persist — while publishing lifecycle events to a bus and routing errors uniformly.

---

## Table of Contents

- [Architecture](#architecture)
- [Key Concepts](#key-concepts)
- [Installation](#installation)
- [Quick Start](#quick-start)
- [Examples](#examples)
  - [HTTP JSON Array](#http-json-array)
  - [YAML Source](#yaml-source)
  - [SQL ResultSet](#sql-resultset)
  - [Custom Transformer Pipeline](#custom-normalizer-pipeline)
  - [Domain Event Mapping](#domain-event-mapping)
  - [Custom Ports (InboxWriter + EventBus)](#custom-ports-inboxwriter--eventbus)
  - [Custom Error Routing](#custom-error-routing)
- [API Reference](#api-reference)
- [Built-in Formats](#built-in-formats)
- [Event Model](#event-model)
- [License](#license)

---

## Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                           Crawler                                │
│                                                                  │
│  PollSource.poll()                                               │
│       │                                                          │
│       ▼                                                          │
│  Deserializer ──► Splitter ──► Serializer                        │
│       │              │             │                              │
│       └──────────────┴─────────────┘                              │
│                      │                                           │
│                      ▼                                           │
│             InboxWriter.receive()                                │
│                      │                                           │
│            ┌─────────┴──────────┐                                │
│            ▼                    ▼                                 │
│     EventBus.emit(         EventBus.emit(                        │
│      SourceItemCaptured)    InboxItemStored)                     │
│                                                                  │
│  Errors ──► ErrorRouter ──► EventBus.emit(FailureEvent)          │
└──────────────────────────────────────────────────────────────────┘
```

The pipeline for a poll-based crawler:

1. **`PollSource.poll()`** — fetches raw data from the external source.
2. **Deserialize** — converts the raw output into a typed intermediate object.
3. **Split** — expands the intermediate object into individual records.
4. **Serialize** — converts each record into a `String` payload.
5. **`InboxWriter.receive()`** — persists each record with an idempotency key.
6. **EventBus** — emits `SourceItemCaptured` and `InboxItemStored` events.

Every step is wrapped in safe error handling; failures are dispatched through the `ErrorRouter`.

---

## Key Concepts

| Concept | Description |
|---|---|
| **`PollSource<R>`** | Interface you implement to fetch raw data (HTTP, DB, file…). |
| **`StreamSource<R>`** | Interface for push-based streaming sources (Kafka, WebSocket…). |
| **`WebhookSource`** | Interface for HTTP webhook endpoints. |
| **`TransformerFormat<T,O>`** | Describes a processing pipeline: deserializer + splitter + serializer. |
| **`Formats`** | Pre-built formats: `JSON_ARRAY`, `YAML_ARRAY`, `RESULT_SET`. |
| **`InboxWriter`** | Interface you implement to persist each record (DB, queue, in-memory…). |
| **`CrawlerPorts`** | Aggregates `InboxWriter`, `EventBusEmitter`, and `ErrorRouter`. |
| **`CrawlerBuilderFactory`** | DSL entry point — composes everything and returns a `CrawlerStrategy`. |
| **`ErrorRouter`** | Maps exception types to handlers. Default mapping provided by `CrawlerErrorRouter`. |

---

## Installation

### Maven (GitHub Packages)

```xml
<dependency>
    <groupId>io.github.spool-framework</groupId>
    <artifactId>crawler</artifactId>
    <version>1.0.2-SNAPSHOT</version>
</dependency>
```

Configure the GitHub Packages repository in your `pom.xml` or `settings.xml`:

```xml
<repositories>
    <repository>
        <id>github</id>
        <url>https://maven.pkg.github.com/Spool-FRAMEWORK/*</url>
    </repository>
</repositories>
```

> Requires **Java 17+**.

---

## Quick Start

```java
// 1. Implement a PollSource that fetches a JSON array
public class MyApiSource implements PollSource<String> {
    @Override
    public String poll() {
        return httpClient.get("https://api.example.com/records");
    }

    @Override
    public String sourceId() {
        return "my-api";
    }
}

// 2. Build and execute the crawler
CrawlerStrategy crawler = CrawlerBuilderFactory.poll(new MyApiSource())
        .withFormat(Formats.JSON_ARRAY)
        .ports(CrawlerPorts.builder()
                .inbox(myInboxWriter)
                .bus(myEventBus)
                .build())
        .create();

crawler.execute();
```

---

## Examples

### HTTP JSON Array

The most common use case: fetching a JSON array from a REST API and storing each element in the inbox.

```java
public class GeminiTradeSource implements PollSource<String> {
    private final HttpClient http = HttpClient.newHttpClient();

    @Override
    public String poll() throws SpoolException {
        try {
            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create("https://api.gemini.com/v1/trades/btcusd?limit_trades=10"))
                    .GET()
                    .build();
            return http.send(request, HttpResponse.BodyHandlers.ofString()).body();
        } catch (Exception e) {
            throw new SourcePollException(sourceId(), e.getMessage(), e);
        }
    }

    @Override
    public String sourceId() {
        return "gemini-btcusd";
    }
}

// Uses InMemoryInboxWriter for local testing
CrawlerStrategy crawler = CrawlerBuilderFactory.poll(new GeminiTradeSource())
        .withFormat(Formats.JSON_ARRAY)
        .ports(CrawlerPorts.builder()
                .inbox(new InMemoryInboxWriter())
                .bus(new InMemoryEventBus())
                .build())
        .create();

crawler.execute();
```

---

### YAML Source

Reading a YAML sequence from a file and splitting each element into individual records.

```java
public class YamlFileSource implements PollSource<String> {
    @Override
    public String poll() throws SpoolException {
        try {
            return Files.readString(Path.of("config/items.yaml"));
        } catch (IOException e) {
            throw new SourcePollException(sourceId(), e.getMessage(), e);
        }
    }

    @Override
    public String sourceId() {
        return "yaml-file";
    }
}

CrawlerStrategy crawler = CrawlerBuilderFactory.poll(new YamlFileSource())
        .withFormat(Formats.YAML_ARRAY)
        .ports(CrawlerPorts.builder()
                .inbox(myInboxWriter)
                .bus(myEventBus)
                .build())
        .create();

crawler.execute();
```

---

### SQL ResultSet

Crawling rows from a JDBC query. Each row is projected into a `Map<String, Object>` and serialized to JSON.

```java
public class OrderSource implements PollSource<ResultSet> {
    private Connection connection;

    @Override
    public PollSource<ResultSet> open() {
        connection = DriverManager.getConnection("jdbc:postgresql://localhost/mydb", "user", "pass");
        return this;
    }

    @Override
    public ResultSet poll() throws SpoolException {
        try {
            return connection.createStatement()
                    .executeQuery("SELECT id, name, total FROM orders WHERE status = 'NEW'");
        } catch (SQLException e) {
            throw new SourcePollException(sourceId(), e.getMessage(), e);
        }
    }

    @Override
    public void close() {
        try { if (connection != null) connection.close(); } catch (SQLException ignored) {}
    }

    @Override
    public String sourceId() {
        return "orders-db";
    }
}

CrawlerStrategy crawler = CrawlerBuilderFactory.poll(new OrderSource())
        .withFormat(Formats.RESULT_SET)
        .ports(CrawlerPorts.builder()
                .inbox(myInboxWriter)
                .bus(myEventBus)
                .build())
        .create();

crawler.execute();
```

---

### Custom Transformer Pipeline

When the built-in formats don't fit, create your own `TransformerFormat` with custom deserialization, splitting, and serialization logic.

```java
TransformerFormat<List<MyRecord>, MyRecord> customFormat = () -> Transformer.of(
        payload -> objectMapper.readValue(payload, new TypeReference<List<MyRecord>>() {}),
        list    -> list.stream(),
        record  -> objectMapper.writeValueAsString(record)
);

CrawlerStrategy crawler = CrawlerBuilderFactory.poll(mySource)
        .withFormat(customFormat)
        .ports(CrawlerPorts.builder()
                .inbox(myInboxWriter)
                .bus(myEventBus)
                .build())
        .create();

crawler.execute();
```

---

### Domain Event Mapping

Automatically deserialize each captured record into a domain event and emit it on the event bus.

```java
// Option A: The record itself implements Event — auto-deserialized with SNAKE_CASE naming
CrawlerStrategy crawler = CrawlerBuilderFactory.poll(mySource)
        .withFormat(Formats.JSON_ARRAY)
        .withDomainEvent(OrderReceived.class)
        .ports(ports)
        .create();

// Option B: Map a DTO to a domain event with a custom function
CrawlerStrategy crawler = CrawlerBuilderFactory.poll(mySource)
        .withFormat(Formats.JSON_ARRAY)
        .withNamingConvention(NamingConvention.CAMEL_CASE)
        .withDomainEvent(OrderDTO.class, (dto, key) ->
                new OrderReceived(dto.id(), dto.total(), key))
        .ports(ports)
        .create();
```

---

### Custom Ports (InboxWriter + EventBus)

Supply your own infrastructure adapters via `CrawlerPorts`:

```java
// Custom InboxWriter backed by a JPA repository
InboxWriter jpaWriter = (payload, idempotencyKey) -> {
    InboxEntity entity = new InboxEntity(payload, idempotencyKey.value());
    repository.save(entity);
    return idempotencyKey;
};

// Kafka-based EventBusEmitter
EventBusEmitter kafkaBus = event ->
    kafkaProducer.send(new ProducerRecord<>("spool-events",
            event.idempotencyKey().value(),
            objectMapper.writeValueAsString(event)));

CrawlerPorts ports = CrawlerPorts.builder()
        .inbox(jpaWriter)
        .bus(kafkaBus)
        .build();

CrawlerStrategy crawler = CrawlerBuilderFactory.poll(new GeminiTradeSource())
        .withFormat(Formats.JSON_ARRAY)
        .ports(ports)
        .create();

crawler.execute();
```

---

### Custom Error Routing

Override the default error routing to add logging, metrics, or custom recovery logic:

```java
ErrorRouter customRouter = new ErrorRouter()
        .on(SourcePollException.class, (e, cause) -> {
            logger.error("Source fetch failed: {}", e.getMessage());
            bus.emit(SourceFetchFailed.builder()
                    .errorMessage(e.getMessage()).build());
        })
        .on(DeserializationException.class, (e, cause) -> {
            logger.warn("Bad payload discarded: {}", e.getMessage());
        })
        .on(InboxWriteException.class, (e, cause) -> {
            metrics.increment("inbox.write.failures");
            bus.emit(InboxItemStoreFailed.builder()
                    .from(cause).errorMessage(e.getMessage()).build());
        });

CrawlerStrategy crawler = CrawlerBuilderFactory.poll(mySource)
        .withFormat(Formats.JSON_ARRAY)
        .ports(CrawlerPorts.builder()
                .inbox(myInboxWriter)
                .bus(myEventBus)
                .errorRouter(customRouter)
                .build())
        .create();

crawler.execute();
```

---

## API Reference

### Source Interfaces

| Interface | Method | Description |
|---|---|---|
| `PollSource<R>` | `R poll()` | Fetch the next payload on demand. |
| `PollSource<R>` | `PollSource<R> open()` | Set up connections before polling (optional). |
| `PollSource<R>` | `String sourceId()` | Unique identifier for the source. |
| `StreamSource<R>` | `void start(Consumer, Consumer)` | Start continuous streaming with callbacks. |
| `StreamSource<R>` | `void stop()` | Stop the stream and release resources. |
| `WebhookSource` | `WebhookRoute bindRoute()` | Return the HTTP route configuration. |

### Builder DSL

| Step | Method | Description |
|---|---|---|
| Entry point | `CrawlerBuilderFactory.poll(source)` | Start building a poll-based crawler. |
| Format | `.withFormat(Formats.JSON_ARRAY)` | Apply a pre-built processing pipeline. |
| Naming | `.withNamingConvention(NamingConvention.CAMEL_CASE)` | Set JSON field naming convention. |
| Domain Events | `.withDomainEvent(MyEvent.class)` | Register auto-deserialized domain events. |
| Ports | `.ports(CrawlerPorts.builder()...build())` | Provide inbox, bus, and error router. |
| Build | `.create()` | Return the configured `CrawlerStrategy`. |

---

## Built-in Formats

| Constant | Input Type | Intermediate | Output | Description |
|---|---|---|---|---|
| `Formats.JSON_ARRAY` | `String` (JSON) | `JsonNode` | `JsonNode` per element | Parse JSON array → split elements → serialize each to JSON. |
| `Formats.YAML_ARRAY` | `String` (YAML) | `JsonNode` | `JsonNode` per element | Parse YAML sequence → split elements → serialize each to JSON. |
| `Formats.RESULT_SET` | `ResultSet` | `ResultSet` | `Map<String, Object>` per row | Project each row → serialize to JSON. |

---

## Event Model

| Event | When Emitted |
|---|---|
| `SourceItemCaptured` | After a record is deserialized, split, and serialized. |
| `InboxItemStored` | After a record is successfully written to the inbox. |
| `SourceFetchFailed` | When `PollSource.poll()` or `open()` throws an exception. |
| `SourceItemCaptureFailed` | When deserialization, splitting, or serialization fails. |
| `InboxItemStoreFailed` | When `InboxWriter.receive()` throws an exception. |

---

## License

Distributed under the [Apache License 2.0](https://www.apache.org/licenses/LICENSE-2.0).
</file>

<file path="src/main/java/software/spool/crawler/api/builder/EventMappingSpecification.java">
package software.spool.crawler.api.builder;

import software.spool.core.adapter.jackson.PayloadDeserializerFactory;
import software.spool.core.model.Event;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.port.bus.EventPublisher;
import software.spool.core.port.serde.NamingConvention;
import software.spool.core.port.serde.PayloadDeserializer;
import software.spool.core.utils.serialization.DomainEventMapping;
import software.spool.crawler.internal.utils.DomainEventEmitter;
import software.spool.crawler.internal.utils.TypedDomainMapping;

import java.util.ArrayList;
import java.util.List;
import java.util.function.BiFunction;

public class EventMappingSpecification {
    private final NamingConvention namingConvention;
    private final List<TypedDomainMapping> domainMappings;
    private final List<String> defaultPartitionAttributes;

    public EventMappingSpecification(NamingConvention namingConvention) {
        this.namingConvention = namingConvention;
        this.domainMappings = new ArrayList<>();
        this.defaultPartitionAttributes = new ArrayList<>();
    }

    private <D> PayloadDeserializer<D> deserializerFor(Class<D> type) {
        return PayloadDeserializerFactory.json().convention(this.namingConvention).as(type);
    }

    public EventMappingSpecification addDomainEvent(Class<? extends Event> eventType, String... partitionAttributes) {
        if (hasConflict())
            throw new IllegalArgumentException("Only one can be used at the same time. Please, use addDomainEvent(...) or addPartitionAttributes(...) but not both.");
        domainMappings.add(new TypedDomainMapping(eventType,
                DomainEventMapping.of(deserializerFor(eventType)),
                List.of(partitionAttributes)));
        return this;
    }

    public <D> EventMappingSpecification addDomainEvent(Class<D> dtoType, BiFunction<D, IdempotencyKey, Event> toEvent, String... partitionAttributes) {
        if (hasConflict())
            throw new IllegalArgumentException("Only one can be used at the same time. Please, use addDomainEvent(...) or addPartitionAttributes(...) but not both.");
        domainMappings.add(new TypedDomainMapping(dtoType,
                DomainEventMapping.of(deserializerFor(dtoType), toEvent),
                List.of(partitionAttributes)));
        return this;
    }

    public EventMappingSpecification addPartitionAttributes(String... attributes) {
        if (hasConflict())
            throw new IllegalArgumentException("Only one can be used at the same time. Please, use addDomainEvent(...) or addPartitionAttributes(...) but not both.");
        defaultPartitionAttributes.addAll(List.of(attributes));
        return this;
    }

    public boolean hasConflict() {
        return !domainMappings.isEmpty() && !defaultPartitionAttributes.isEmpty();
    }

    public DomainEventEmitter buildEmitter(EventPublisher bus) {
        return new DomainEventEmitter(bus, domainMappings);
    }

    public List<String> partitionAttributes() {
        return defaultPartitionAttributes;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/port/source/PollSource.java">
package software.spool.crawler.api.port.source;

import software.spool.core.exception.SpoolException;
import software.spool.crawler.api.utils.NormalizerFormat;

/**
 * A data source that is polled on demand to fetch the next payload.
 *
 * <p>
 * The crawler calls {@link #open()} once before starting the polling cycle
 * and {@link #fetch()} to retrieve the raw data. Results are then fed into the
 * configured {@link NormalizerFormat} pipeline for
 * deserialization, splitting, and serialization.
 * </p>
 *
 * <p>
 * Implementations are responsible for managing their own connection lifecycle
 * inside {@link #open()} and {@link #close()} (inherited from
 * {@link java.lang.AutoCloseable} via {@link Source}).
 * </p>
 *
 * @param <R> the raw type returned by {@link #fetch()}
 */
public interface PollSource<R> extends Source<R> {
    /**
     * Fetches the next payload from the source.
     *
     * <p>
     * This method is called by the crawler during each execution cycle.
     * Implementors should return the current snapshot of data from their source
     * (e.g. fetch a REST endpoint, query a database, read a file, etc.).
     * </p>
     *
     * @return the raw payload; must not be {@code null}
     * @throws SpoolException if the payload could not be retrieved
     */
    R fetch() throws SpoolException;

    /**
     * Opens (or re-opens) the source and returns itself.
     *
     * <p>
     * Override to perform any connection setup before polling begins. The
     * default implementation is a no-op that simply returns {@code this}.
     * </p>
     *
     * @return this source instance, ready to be polled
     */
    default PollSource<R> open() {
        return this;
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/port/source/StreamSource.java">
package software.spool.crawler.api.port.source;

import software.spool.core.exception.SpoolException;

import java.util.function.Consumer;

/**
 * A data source that pushes records continuously via a callback-based streaming
 * API.
 *
 * <p>
 * Unlike {@link PollSource}, the crawler does not control the frequency of
 * data arrival. Instead, the source itself drives the flow by calling the
 * {@code onMessage} consumer whenever a new item is available and
 * {@code onError} when a non-fatal error occurs.
 * </p>
 *
 * <p>
 * The source must be started with {@link #start(Consumer, Consumer)} and
 * can be gracefully stopped with {@link #stop()}.
 * </p>
 *
 * @param <R> the raw message type delivered to {@code onMessage}
 */
public interface StreamSource<R> extends Source {
    /**
     * Starts the stream and begins delivering messages to the provided callbacks.
     *
     * <p>
     * Implementations should set up any underlying connection (e.g. subscribe
     * to a Kafka topic, open a WebSocket) and start dispatching to
     * {@code onMessage} as records arrive.
     * </p>
     *
     * @param onMessage callback invoked for each captured source item;
     *                  must not be {@code null}
     * @param onError   callback invoked for non-fatal errors during streaming;
     *                  must not be {@code null}
     * @throws SpoolException if the stream could not be started
     */
    void start(Consumer<R> onMessage, Consumer<Exception> onError) throws SpoolException;

    /**
     * Stops the stream and releases any underlying resources.
     *
     * <p>
     * After this call, the {@code onMessage} and {@code onError} callbacks
     * registered in {@link #start} must no longer be invoked.
     * </p>
     */
    void stop();
}
</file>

<file path="src/main/java/software/spool/crawler/internal/control/steps/BuildAndStoreEnvelopeStep.java">
package software.spool.crawler.internal.control.steps;

import software.spool.core.exception.DuplicateEventException;
import software.spool.core.model.EnvelopeStatus;
import software.spool.core.model.vo.*;
import software.spool.core.pipeline.PipelineContext;
import software.spool.core.pipeline.Step;
import software.spool.core.port.serde.RecordSerializer;
import software.spool.crawler.api.port.InboxWriter;
import software.spool.crawler.internal.utils.TypedDomainMapping;

import javax.management.AttributeNotFoundException;
import java.time.Instant;
import java.util.List;
import java.util.Optional;

public class BuildAndStoreEnvelopeStep implements Step<PipelineContext, PipelineContext> {
    private final InboxWriter inboxWriter;
    private final RecordSerializer<PartitionKeySchema> serializer;
    private final List<String> defaultPartitionAttributes;

    public BuildAndStoreEnvelopeStep(InboxWriter inboxWriter, RecordSerializer<PartitionKeySchema> serializer, List<String> defaultPartitionAttributes) {
        this.inboxWriter = inboxWriter;
        this.serializer = serializer;
        this.defaultPartitionAttributes = defaultPartitionAttributes;
    }

    @Override
    public PipelineContext apply(PipelineContext ctx) throws AttributeNotFoundException {
        IdempotencyKey key = inboxWriter.receive(buildEnvelope(ctx));
        if (key == null)
            throw new DuplicateEventException(ctx.require(CapturedPayloadKeys.CAPTURED_EVENT).idempotencyKey());
        return ctx.with(CapturedPayloadKeys.RECEIVED_KEY, key);
    }

    private Envelope buildEnvelope(PipelineContext ctx) throws AttributeNotFoundException {
        return new Envelope(
                ctx.require(CapturedPayloadKeys.CAPTURED_EVENT).idempotencyKey(),
                buildMetadata(ctx),
                ctx.require(CapturedPayloadKeys.MEDIA_TYPE),
                ctx.require(CapturedPayloadKeys.PAYLOAD),
                EnvelopeStatus.CAPTURED, 0, Instant.now(), null);
    }

    private EventMetadata buildMetadata(PipelineContext ctx) throws AttributeNotFoundException {
        Optional<TypedDomainMapping> matched = ctx.require(CapturedPayloadKeys.DOMAIN_MAPPING);
        EventMetadata metadata = new EventMetadata()
                .set(EventMetadataKey.SOURCE, ctx.require(CapturedPayloadKeys.SOURCE_ID))
                .set(EventMetadataKey.PARTITION_SCHEMA, serializer.serialize(buildPartitionSchema(ctx)))
                .set(EventMetadataKey.CORRELATION_ID, ctx.require(CapturedPayloadKeys.CAPTURED_EVENT).correlationId());
        matched.map(TypedDomainMapping::targetType)
                .ifPresent(type -> metadata.set(EventMetadataKey.TYPE, type.toString()));
        return metadata;
    }

    private PartitionKeySchema buildPartitionSchema(PipelineContext ctx) throws AttributeNotFoundException {
        Optional<TypedDomainMapping> matched = ctx.require(CapturedPayloadKeys.DOMAIN_MAPPING);
        return PartitionKeySchema.of(
                ctx.require(CapturedPayloadKeys.SOURCE_ID),
                matched.map(TypedDomainMapping::targetType).orElse(null),
                matched.map(TypedDomainMapping::partitionAttributes).orElse(defaultPartitionAttributes));
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/port/decorator/SafePollSource.java">
package software.spool.crawler.internal.port.decorator;

import software.spool.core.exception.SourcePollException;
import software.spool.core.exception.SpoolException;
import software.spool.crawler.api.port.source.PollSource;

/**
 * Decorator for {@link PollSource} that normalises unchecked exceptions into
 * typed {@link SourcePollException} instances.
 *
 * <p>
 * If the delegate's {@link #fetch()} method throws a
 * {@link SpoolException} subclass, it is re-thrown as-is. Any other
 * {@link Exception} is wrapped in a new {@link SourcePollException}. This
 * guarantees the crawler strategy always receives typed exceptions that can be
 * </p>
 *
 * @param <R> the raw type produced by the wrapped source
 */
public class SafePollSource<R> implements PollSource<R> {
    private final PollSource<R> source;

    private SafePollSource(PollSource<R> source) {
        this.source = source;
    }

    /**
     * Creates a new {@code SafePollSource} wrapping the given delegate.
     *
     * @param <R>    the raw type produced by the source
     * @param source the source to wrap; must not be {@code null}
     * @return a new {@code SafePollSource} instance
     */
    public static <R> SafePollSource<R> of(PollSource<R> source) {
        return new SafePollSource<>(source);
    }

    @Override
    public R fetch() throws SpoolException {
        try {
            return source.fetch();
        } catch (SpoolException e) {
            throw e;
        } catch (Exception e) {
            throw new SourcePollException(sourceId(), e.getMessage(), e);
        }
    }

    @Override
    public String sourceId() {
        return source.sourceId();
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/utils/factory/PayloadSplitterFactory.java">
package software.spool.crawler.internal.utils.factory;

import com.fasterxml.jackson.databind.JsonNode;
import software.spool.core.exception.SplitException;
import software.spool.crawler.api.port.PayloadSplitter;

import java.sql.ResultSet;
import java.sql.ResultSetMetaData;
import java.sql.SQLException;
import java.util.Iterator;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Spliterators;
import java.util.stream.Stream;
import java.util.stream.StreamSupport;

/**
 * Factory methods for common {@link PayloadSplitter} implementations.
 *
 * <p>
 * All returned splitters are stateless lambdas or inner-class instances
 * and can be safely reused across multiple executions.
 * </p>
 */
public class PayloadSplitterFactory {

    private PayloadSplitterFactory() {
        // utility class
    }

    /**
     * Returns a splitter that streams the elements of a {@link JsonNode} array.
     *
     * @return a splitter for JSON arrays; throws {@link SplitException} if
     *         the parsed node is not an array
     */
    public static PayloadSplitter<JsonNode, JsonNode> jsonArray() {
        return parsed -> {
            if (parsed.isArray())
                return StreamSupport.stream(parsed.spliterator(), false);
            throw new SplitException("Expected JsonNode array, got: " + parsed.getNodeType().name(),
                    parsed.toString());
        };
    }

    /**
     * Returns a splitter that wraps a single parsed value in a one-element stream,
     * effectively performing no splitting.
     *
     * @param <T> the type of the value
     * @return a passthrough splitter
     */
    public static <T> PayloadSplitter<T, T> single() {
        return Stream::of;
    }

    /**
     * Returns a splitter that iterates over a JDBC {@link ResultSet} and projects
     * each row into a {@code Map<String, Object>} keyed by column label.
     *
     * @return a splitter for {@code ResultSet} sources
     */
    public static PayloadSplitter<ResultSet, Map<String, Object>> resultSet() {
        return rs -> StreamSupport.stream(
                Spliterators.spliteratorUnknownSize(
                        new ResultSetIterator(rs),
                        0),
                false);
    }

    public static PayloadSplitter<JsonNode, JsonNode> withRootPath(String rootPath) {
        return parsed -> {
            JsonNode target = parsed.at(rootPath);
            if (target.isMissingNode())
                throw new SplitException(
                        "Path not found: " + rootPath, parsed.toString());
            if (!target.isArray())
                throw new SplitException(
                        "Expected array at " + rootPath + ", got: " + target.getNodeType().name(),
                        target.toString());
            return StreamSupport.stream(target.spliterator(), false);
        };
    }

    /**
     * {@link Iterator} that lazily advances a {@link ResultSet} and converts each
     * row to a {@code Map<String, Object>}.
     */
    private static class ResultSetIterator implements Iterator<Map<String, Object>> {
        private final ResultSet rs;
        private Map<String, Object> nextRow = null;
        private boolean hasNext = false;

        public ResultSetIterator(ResultSet rs) {
            this.rs = rs;
        }

        @Override
        public boolean hasNext() {
            if (!hasNext) {
                try {
                    hasNext = rs.next();
                    if (hasNext) {
                        nextRow = rowToMap(rs);
                    }
                } catch (SQLException e) {
                    throw new SplitException("ResultSet hasNext failed", null);
                }
            }
            return hasNext;
        }

        @Override
        public Map<String, Object> next() {
            if (!hasNext) {
                throw new java.util.NoSuchElementException();
            }
            hasNext = false;
            return nextRow;
        }

        private Map<String, Object> rowToMap(ResultSet rs) throws SQLException {
            ResultSetMetaData meta = rs.getMetaData();
            Map<String, Object> row = new LinkedHashMap<>();
            for (int i = 1; i <= meta.getColumnCount(); i++) {
                row.put(meta.getColumnLabel(i), rs.getObject(i));
            }
            return row;
        }
    }
}
</file>

<file path=".github/workflows/publish-develop.yml">
name: Publish to Spool-FRAMEWORK Packages
on:
  push:
    branches: [ develop ]
  workflow_dispatch:

permissions:
  packages: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - name: Publish to GitHub Packages
        run: mvn deploy -B --no-transfer-progress -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_ACTOR: ${{ github.actor }}
</file>

<file path="src/main/java/software/spool/crawler/internal/port/decorator/SafeInboxWriter.java">
package software.spool.crawler.internal.port.decorator;

import software.spool.core.exception.InboxWriteException;
import software.spool.core.exception.SpoolException;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.model.vo.Envelope;
import software.spool.crawler.api.port.InboxWriter;

/**
 * Decorator for {@link InboxWriter} that normalises unchecked exceptions into
 * typed {@link InboxWriteException} instances.
 *
 * <p>
 * {@link SpoolException} subclass, it is re-thrown as-is. Any other
 * {@link Exception} is wrapped in a new {@link InboxWriteException}.
 * </p>
 */
public class SafeInboxWriter implements InboxWriter {
    private final InboxWriter inbox;

    private SafeInboxWriter(InboxWriter inbox) {
        this.inbox = inbox;
    }

    /**
     * Creates a new {@code SafeInboxWriter} wrapping the given delegate.
     *
     * @param inbox the inbox writer to wrap; must not be {@code null}
     * @return a new {@code SafeInboxWriter} instance
     */
    public static SafeInboxWriter of(InboxWriter inbox) {
        return new SafeInboxWriter(inbox);
    }

    @Override
    public IdempotencyKey receive(Envelope envelope) throws InboxWriteException {
        try {
            return inbox.receive(envelope);
        } catch (SpoolException e) {
            throw e;
        } catch (Exception e) {
            throw new InboxWriteException(e.getMessage(), e);
        }
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/port/decorator/SafePayloadDeserializer.java">
package software.spool.crawler.internal.port.decorator;

import software.spool.core.exception.DeserializationException;
import software.spool.core.exception.SpoolException;
import software.spool.core.port.serde.PayloadDeserializer;

public class SafePayloadDeserializer<T> implements PayloadDeserializer<T> {
    private final PayloadDeserializer<T> deserializer;

    private SafePayloadDeserializer(PayloadDeserializer<T> deserializer) {
        this.deserializer = deserializer;
    }

    public static <T> SafePayloadDeserializer<T> of(PayloadDeserializer<T> deserializer) {
        return new SafePayloadDeserializer<>(deserializer);
    }

    @Override
    public T deserialize(byte[] payload) throws DeserializationException {
        try {
            return deserializer.deserialize(payload);
        } catch (SpoolException e) {
            throw e;
        } catch (Exception e) {
            throw new DeserializationException(new String(payload), e.getMessage());
        }
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/utils/DomainEventEmitter.java">
package software.spool.crawler.internal.utils;

import software.spool.core.exception.DeserializationException;
import software.spool.core.exception.SerializationException;
import software.spool.core.model.Event;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.port.bus.EventPublisher;

import java.nio.charset.StandardCharsets;
import java.util.List;
import java.util.Optional;

public class DomainEventEmitter {
    private final EventPublisher bus;
    private final List<TypedDomainMapping> domainMappings;

    public DomainEventEmitter(EventPublisher bus, List<TypedDomainMapping> domainMappings) {
        this.bus = bus;
        this.domainMappings = domainMappings;
    }

    public Optional<TypedDomainMapping> emit(byte[] payload, IdempotencyKey idempotencyKey) {
        if (domainMappings.isEmpty()) return Optional.empty();
        for (TypedDomainMapping typed : domainMappings) {
            try {
                Event event = typed.mapping().resolve(payload, idempotencyKey);
                bus.publish(event);
                return Optional.of(typed);
            } catch (DeserializationException | SerializationException ignored) {}
        }
        throw new DeserializationException(new String(payload), "No matching domain event mapper found");
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/utils/CrawlerPorts.java">
package software.spool.crawler.api.utils;

import software.spool.core.port.bus.EventPublisher;
import software.spool.core.port.decorator.SafeEventPublisher;
import software.spool.crawler.api.port.InboxWriter;
import software.spool.crawler.internal.port.decorator.SafeInboxWriter;

/**
 * Aggregates the external ports required by a crawler strategy into a single
 * immutable value object.
 *
 * <p>
 * Use {@link #builder()} to construct instances:
 * </p>
 * 
 * <pre>{@code
 * CrawlerPorts ports = CrawlerPorts.builder()
 *         .bus(myEventBus)
 *         .inbox(myInboxWriter)
 *         .errorRouter(myErrorRouter)
 *         .build();
 * }</pre>
 */
public class CrawlerPorts {
    private final InboxWriter inboxWriter;
    private final EventPublisher bus;

    private CrawlerPorts(Builder builder) {
        this.inboxWriter = builder.inboxWriter;
        this.bus = builder.bus;
    }

    /**
     * Returns the {@link InboxWriter} port.
     *
     * @return the inbox writer; may be {@code null} if not configured
     */
    public InboxWriter inboxWriter() {
        return inboxWriter;
    }

    public EventPublisher bus() {
        return bus;
    }


    /**
     * Returns a new {@link Builder} for constructing a {@code CrawlerPorts}
     * instance.
     *
     * @return a new builder
     */
    public static Builder builder() {
        return new Builder();
    }

    /**
     * Fluent builder for {@link CrawlerPorts}.
     */
    public static class Builder {
        private InboxWriter inboxWriter;
        private EventPublisher bus;

        /** Creates a new empty builder. */
        Builder() {
        }

        /**
         * Sets the {@link InboxWriter} port.
         *
         * @param inboxWriter the inbox writer to use
         * @return this builder for chaining
         */
        public Builder inbox(InboxWriter inboxWriter) {
            this.inboxWriter = SafeInboxWriter.of(inboxWriter);
            return this;
        }

        /**
         * Sets the {@link EventPublisher} port.
         *
         * @param bus the event bus emitter to use
         * @return this builder for chaining
         */
        public Builder bus(EventPublisher bus) {
            this.bus = SafeEventPublisher.of(bus);
            return this;
        }

        /**
         * Builds and returns the configured {@link CrawlerPorts} instance.
         *
         * @return a new {@code CrawlerPorts}
         */
        public CrawlerPorts build() {
            return new CrawlerPorts(this);
        }
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/builder/CrawlerBuilderFactory.java">
package software.spool.crawler.api.builder;

import software.spool.core.adapter.watchdog.HttpWatchdogClient;
import software.spool.core.adapter.resilience.ResilienceWatchdogClient;
import software.spool.core.model.watchdog.ModuleIdentity;
import software.spool.core.port.watchdog.ModuleHeartBeat;
import software.spool.core.utils.polling.PollingHeartbeat;
import software.spool.crawler.api.port.source.PollSource;

import java.util.Objects;

public final class CrawlerBuilderFactory {
    private CrawlerBuilderFactory() {}

    public static <R> PollingCrawlerBuilder<R> poll(PollSource<R> source) {
        return new Configuration().poll(source);
    }

    public static Configuration watchdog(String url, String moduleId) {
        return new Configuration(url, moduleId);
    }

    public static final class Configuration {
        private final String watchdogUrl;
        private final String moduleId;

        private Configuration(String watchdogUrl, String moduleId) {
            this.watchdogUrl = watchdogUrl;
            this.moduleId = moduleId;
        }

        private Configuration() {
            this(null, "crawler");
        }

        public <R> PollingCrawlerBuilder<R> poll(PollSource<R> source) {
            return new PollingCrawlerBuilder<>(source, buildHeartbeat(watchdogUrl, moduleId));
        }
    }

    private static ModuleHeartBeat buildHeartbeat(String watchdogUrl, String moduleId) {
        return Objects.isNull(watchdogUrl) ?
                ModuleHeartBeat.NOOP : new PollingHeartbeat(
                new ResilienceWatchdogClient(new HttpWatchdogClient(watchdogUrl)),
                ModuleIdentity.of(moduleId)
        );
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/Crawler.java">
package software.spool.crawler.api;

import software.spool.core.model.spool.SpoolModule;
import software.spool.core.model.spool.SpoolNode;
import software.spool.core.port.health.ModuleHealthPayload;
import software.spool.core.port.watchdog.ModuleHeartBeat;
import software.spool.core.utils.polling.CancellationToken;
import software.spool.core.utils.routing.ErrorRouter;
import software.spool.crawler.api.strategy.CrawlerStrategy;

import java.util.Objects;

public class Crawler implements SpoolModule {
    private final CrawlerStrategy strategy;
    private volatile CancellationToken token;
    private final ErrorRouter errorRouter;
    private final ModuleHeartBeat heartBeat;

    public Crawler(CrawlerStrategy strategy, ErrorRouter errorRouter, ModuleHeartBeat heartBeat) {
        this.strategy = strategy;
        this.errorRouter = errorRouter;
        this.heartBeat = heartBeat;
        this.token = CancellationToken.NOOP;
    }

    @Override
    public void start(SpoolNode.StartPermit permit) {
        if (token.isActive()) return;
        Objects.requireNonNull(permit);
        token = CancellationToken.create();
        try {
            heartBeat.start();
            strategy.execute(token);
        } catch (Exception e) { errorRouter.dispatch(e); }
    }

    @Override
    public void stop(SpoolNode.StartPermit permit) {
        if (!token.isActive()) return;
        Objects.requireNonNull(permit);
        token.cancel();
        heartBeat.stop();
        token = CancellationToken.NOOP;
    }

    @Override
    public ModuleHealthPayload checkHealth() {
        return token.isActive() ? ModuleHealthPayload.healthy(heartBeat.identity().moduleId()) : ModuleHealthPayload.degraded(heartBeat.identity().moduleId(), null);
    }
}
</file>

<file path="src/main/java/software/spool/crawler/internal/strategy/PollingCrawlerStrategy.java">
package software.spool.crawler.internal.strategy;

import software.spool.core.exception.*;
import software.spool.core.port.bus.Handler;
import software.spool.core.utils.polling.CancellationToken;
import software.spool.core.utils.polling.PollingConfiguration;
import software.spool.core.utils.routing.ErrorRouter;
import software.spool.crawler.api.strategy.CrawlerStrategy;
import software.spool.crawler.internal.utils.factory.Normalizer;
import software.spool.crawler.api.port.source.PollSource;

import java.time.Duration;
import java.util.Objects;

public class PollingCrawlerStrategy<I, P, E, R> implements CrawlerStrategy {
    private final PollSource<I> source;
    private final Normalizer<P, E, R> normalizer;
    private final ErrorRouter errorRouter;
    private final Handler<byte[]> itemmCapturedHandler;
    private final PollingConfiguration pollingConfiguration;

    public PollingCrawlerStrategy(PollSource<I> source, Normalizer<P, E, R> normalizer,
                                  Handler<byte[]> itemmCapturedHandler,
                                  PollingConfiguration pollingConfiguration, ErrorRouter errorRouter) {
        this.source = Objects.requireNonNull(source);
        this.normalizer = Objects.requireNonNull(normalizer);
        this.errorRouter = Objects.requireNonNull(errorRouter);

        this.itemmCapturedHandler = Objects.requireNonNull(itemmCapturedHandler);
        this.pollingConfiguration = Objects.requireNonNullElse(pollingConfiguration,
                PollingConfiguration.every(Duration.ofSeconds(10)));
    }

    @Override
    public void execute(CancellationToken token) throws SpoolException {
        pollingConfiguration.scheduler().schedule(
                () -> { try (PollSource<I> openedSource = this.source.open()) {
                        normalizer.normalize(openedSource.fetch())
                            .takeWhile(p -> token.isActive())
                            .forEach(itemmCapturedHandler::handle);
                    } catch (Exception e) { errorRouter.dispatch(e); }
                },
                pollingConfiguration.policy(),
                token
        );
    }
}
</file>

<file path=".docker/docker-compose.yml">
name: spool-infrastructure

services:

  postgres:
    image: postgres:16-alpine
    container_name: spool-postgres
    environment:
      POSTGRES_USER: spool
      POSTGRES_PASSWORD: spool
      POSTGRES_DB: spool
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U spool -d spool" ]
      interval: 10s
      timeout: 5s
      retries: 5

  kafka:
    image: apache/kafka:3.9.0
    container_name: spool-kafka
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://host.docker.internal:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@spool-kafka:9093
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
    ports:
      - "9092:9092"

  tempo:
    image: grafana/tempo:2.6.1
    container_name: spool-tempo
    command: [ "-config.file=/etc/tempo.yaml" ]
    volumes:
      - ./tempo.yaml:/etc/tempo.yaml:ro
    ports:
      - "4317:4317"
      - "4318:4318"
      - "3200:3200"

  loki:
    image: grafana/loki:3.3.0
    container_name: spool-loki
    command: [ "-config.file=/etc/loki/loki.yaml" ]
    volumes:
      - ./loki.yaml:/etc/loki/loki.yaml:ro
    ports:
      - "3100:3100"

  mimir:
    image: grafana/mimir:2.14.0
    container_name: spool-mimir
    command: [ "-config.file=/etc/mimir/mimir.yaml" ]
    volumes:
      - ./mimir.yaml:/etc/mimir/mimir.yaml:ro
    ports:
      - "9009:9009"

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.123.0
    container_name: spool-otel-collector
    command: [ "--config=/etc/otel-collector.yaml" ]
    volumes:
      - ./otel-collector.yaml:/etc/otel-collector.yaml:ro
    ports:
      - "4319:4317"
      - "4320:4318"
    depends_on:
      - tempo
      - loki
      - mimir

  grafana:
    image: grafana/grafana:11.4.0
    container_name: spool-grafana
    volumes:
      - ./grafana/datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml:ro
    ports:
      - "3000:3000"
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_INSTALL_PLUGINS=https://storage.googleapis.com/integration-artifacts/grafana-exploretraces-app/grafana-exploretraces-app-latest.zip;grafana-traces-app
    depends_on:
      - tempo
      - loki
      - mimir
</file>

<file path="src/main/java/software/spool/crawler/api/builder/PollingCrawlerBuilder.java">
package software.spool.crawler.api.builder;

import software.spool.core.port.serde.NamingConvention;
import software.spool.core.port.watchdog.ModuleHeartBeat;
import software.spool.crawler.api.Crawler;
import software.spool.crawler.api.port.source.PollSource;
import software.spool.crawler.api.utils.NormalizerFormat;
import software.spool.crawler.internal.port.decorator.SafePollSource;
import software.spool.crawler.internal.utils.factory.Normalizer;

public class PollingCrawlerBuilder<I> {

    final PollSource<I> source;
    final ModuleHeartBeat heartBeat;
    final SourceFacet<I> sourceFacet;
    final MappingFacet<I> mappingFacet;
    final ObservabilityFacet<I> observabilityFacet;

    public PollingCrawlerBuilder(PollSource<I> source, ModuleHeartBeat heartBeat) {
        this.source = SafePollSource.of(source);
        this.heartBeat = heartBeat;
        this.sourceFacet = new SourceFacet<>(this);
        this.mappingFacet = new MappingFacet<>(this, new EventMappingSpecification(NamingConvention.SNAKE_CASE));
        this.observabilityFacet = new ObservabilityFacet<>(this);
    }

    public SourceFacet<I> source() {
        return sourceFacet;
    }

    public MappingFacet<I> mapping() {
        return mappingFacet;
    }

    public ObservabilityFacet<I> observability() {
        return observabilityFacet;
    }

    public <P, E, R> Crawler createWith(Normalizer<P, E, R> normalizer) {
        return new PollingCrawlerAssembler<>(this).assemble(normalizer);
    }

    public <P, E, R> Crawler createWith(NormalizerFormat<P, E, R> format) {
        return createWith(format.pipelineWith(sourceFacet.enrichRules, sourceFacet.rootPath));
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/utils/CrawlerErrorRouter.java">
package software.spool.crawler.api.utils;

import software.spool.core.adapter.logging.LoggerFactory;
import software.spool.core.port.bus.EventPublisher;
import software.spool.core.port.logging.Logger;
import software.spool.core.utils.routing.ErrorRouter;
import software.spool.crawler.api.Crawler;


public class CrawlerErrorRouter {

    private CrawlerErrorRouter() {
        // utility class
    }

    /**
     * Creates the default {@link ErrorRouter} wired to the given event bus.
     *
     * @param bus the event bus emitter used for publishing failure events
     * @return a pre-configured error router
     */
    public static ErrorRouter defaults(EventPublisher bus) {
        Logger log = LoggerFactory.getLogger(Crawler.class);
        return new ErrorRouter()
                .orElse((e, cause) -> log.error(e.getMessage()));
    }
}
</file>

<file path="src/main/java/software/spool/crawler/api/strategy/CrawlerStrategy.java">
package software.spool.crawler.api.strategy;

import software.spool.core.exception.SpoolException;
import software.spool.core.utils.polling.CancellationToken;
import software.spool.crawler.api.builder.CrawlerBuilderFactory;

/**
 * Core abstraction representing a single crawler execution cycle.
 *
 * <p>
 * A {@code CrawlerStrategy} encapsulates the complete logic for one run:
 * fetching data from a source, transforming it through the configured pipeline,
 * and delivering the resulting records to the inbox and event bus.
 * </p>
 *
 * <p>
 * The typical entry point for obtaining a pre-configured strategy is the
 * fluent DSL provided by {@link CrawlerBuilderFactory}:
 * </p>
 * 
 * <pre>{@code
 * CrawlerStrategy strategy = Crawlers.poll(mySource)
 *         .withFormat(Formats.JSON_ARRAY)
 *         .inbox(myInboxWriter)
 *         .create();
 *
 * strategy.execute();
 * }</pre>
 */
public interface CrawlerStrategy {
    void execute(CancellationToken token) throws SpoolException;
}
</file>

<file path="src/main/java/software/spool/crawler/api/port/InboxWriter.java">
package software.spool.crawler.api.port;

import software.spool.core.exception.*;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.model.vo.Envelope;

public interface InboxWriter {
    /**
     * Stores a serialized payload in the inbox, using the provided idempotency key
     * to prevent duplicate entries.
     *
     * @return the identifier assigned to the stored entry
     * @throws InboxWriteException if the entry could not be persisted
     * @throws DuplicateEventException if the entry was duplicated
     */
    IdempotencyKey receive(Envelope envelope) throws InboxWriteException, DuplicateEventException;
}
</file>

<file path=".github/workflows/gb-pages.yml">
name: Deploy Javadoc → GitHub Pages
on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy-docs:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - name: Generate Javadoc
        run: mvn javadoc:javadoc -DskipTests -Dquiet=true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_ACTOR: ${{ github.actor }}

      - name: Create .nojekyll
        run: touch target/reports/apidocs/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./target/reports/apidocs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
</file>

<file path="src/main/java/software/spool/crawler/Main.java">
package software.spool.crawler;

import software.spool.core.adapter.memory.InMemoryEventBus;
import software.spool.core.adapter.otel.OTELConfig;
import software.spool.core.model.event.SourcePayloadCaptured;
import software.spool.core.model.spool.SpoolNode;
import software.spool.core.model.vo.IdempotencyKey;
import software.spool.core.port.serde.NamingConvention;
import software.spool.core.utils.media.MediaTypes;
import software.spool.core.utils.polling.PollingConfiguration;
import software.spool.crawler.api.Crawler;
import software.spool.crawler.api.builder.CrawlerBuilderFactory;
import software.spool.crawler.api.utils.CrawlerErrorRouter;
import software.spool.crawler.api.utils.CrawlerPorts;
import software.spool.crawler.api.utils.StandardNormalizer;
import software.spool.crawler.internal.adapter.http.HTTPPollSource;

import java.time.Duration;
import java.util.List;

public class Main {
    public static void main(String[] args) throws Exception {
        OTELConfig.init("crawler");
        InMemoryEventBus broker = new InMemoryEventBus();
        broker.subscribe(SourcePayloadCaptured.class, System.out::println);

        Crawler with = CrawlerBuilderFactory.poll(new HTTPPollSource("http://plytrox.com:8000/events?limit=10", "test"))
                .source()
                    .schedule(PollingConfiguration.every(Duration.ofSeconds(60)))
                    .ports(CrawlerPorts.builder()
                            .inbox(e -> IdempotencyKey.of("test", e.payload()))
                            .bus(broker).build())
                    .mediaType(MediaTypes.PDF)
                    .enrichRules(List.of())
                    .and()
                .mapping()
                    .convention(NamingConvention.SNAKE_CASE)
                    .and()
                .observability()
                    .withErrorRouter(CrawlerErrorRouter.defaults(broker))
                    .and()
                .createWith(StandardNormalizer.JSON_OBJECT);
        SpoolNode.create().register(with).start();
    }
}
</file>

<file path="pom.xml">
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.spool-framework</groupId>
    <artifactId>crawler</artifactId>
    <version>1.0.6-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>${project.groupId}:${project.artifactId}</name>
    <description>${project.artifactId} framework module for event driven systems</description>
    <url>https://github.com/Spool-FRAMEWORK/${project.artifactId}</url>

    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Javier Castilla</name>
            <email>javier.casno@gmail.com</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git@github.com:Spool-FRAMEWORK/${project.artifactId}.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/Spool-FRAMEWORK/${project.artifactId}.git</developerConnection>
        <url>https://github.com/Spool-FRAMEWORK/${project.artifactId}</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>github</id>
            <name>Spool-FRAMEWORK GitHub Packages</name>
            <url>https://maven.pkg.github.com/Spool-FRAMEWORK/*</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <distributionManagement>
        <snapshotRepository>
            <id>github</id>
            <name>Spool-FRAMEWORK GitHub Packages</name>
            <url>https://maven.pkg.github.com/Spool-FRAMEWORK/crawler</url>
        </snapshotRepository>
    </distributionManagement>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals><goal>jar-no-fork</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.11.2</version>
                <configuration>
                    <failOnError>false</failOnError>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals><goal>jar</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <pluginRepositories>
                <pluginRepository>
                    <id>central</id>
                    <name>Maven Central</name>
                    <url>https://repo.maven.apache.org/maven2</url>
                    <releases><enabled>true</enabled></releases>
                    <snapshots><enabled>false</enabled></snapshots>
                </pluginRepository>
            </pluginRepositories>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals><goal>sign</goal></goals>
                            </execution>
                        </executions>
                        <configuration>
                            <gpgArguments>
                                <arg>--batch</arg>
                                <arg>--yes</arg>
                                <arg>--pinentry-mode</arg>
                                <arg>loopback</arg>
                            </gpgArguments>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.7.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <dependencies>
        <dependency>
            <groupId>io.github.spool-framework</groupId>
            <artifactId>core</artifactId>
            <version>1.0.18-SNAPSHOT</version>
        </dependency>
    </dependencies>
</project>
</file>

</files>
